<!--管理者側の商品詳細ページ-->
<h2>商品詳細</h2>
<div class="container mt-5 mb-5">
	<div class="row text-center text-xs-center text-sm-left text-md-left ">
    <div class="justify-content-center">
      <%= image_tag @item.get_image(100,100) %>
    </div>
    
    <div class="col-xs-12 col-sm-6 col-md-6 item-details mx-auto">
      <div class="d-flex justify-content-between">商品名
        <h2>
          <strong><%= @item.name %></strong>
        </h2>
      </div>
        
      <div class="d-flex justify-content-between">商品説明
        <h4>
          <!--https://qiita.com/mmaumtjgj/items/1b672f3accd37387b2f3-->
          <%= simple_format(@item.introduction) %>
        </h4>
      </div>
          
      <div class="d-flex justify-content-between">ジャンル
        <h3><%=@genre.name %></h3>
      </div>
          
      <% if @item.is_active == true %>
        <div class="d-flex justify-content-between">税込価格<br>(税抜価格)
          <h3>
            <!--.to_s(:delimited)は3桁ごとにカンマ区切りで表示してくれる便利なメソッドです-->
            <%= @item.add_tax_price.to_s(:delimited) %>
            (<%= @item.price.to_s(:delimited) %>)円
          </h3>
        </div>
          
        <div class="d-flex justify-content-between">販売ステータス
          <p class="text-success">販売中</p>
        </div>
        
      <% else %>
        <div class="d-flex justify-content-between">販売ステータス
          <h3 class="text-danger">販売停止中</h3>
        </div>
        
      <% end %>
      
      <%= link_to "編集する", edit_admin_item_path, class:"btn btn-success" %>
      
    </div>
  </div>
</div>