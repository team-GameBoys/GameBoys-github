<div class="container">
  <h2 class="my-4 ml-5 px-5"><span class="bg-light"> ログイン </span></h2>
  <div class="row">
    <div class="px-2 mx-auto">
      <h4 class="my-4 mr-5 pr-5"><span class="bg-light">会員の方はこちらからログイン</span></h4>
            <div class="pl-5">
              <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
              <table cellpadding="15">
                <tr>
                  <td><%= f.label :メールアドレス %></td>
                  <td><%= f.email_field :email, autofocus: true, autocomplete: "email" %></td>
                </tr>
                <tr>
                  <td><%= f.label :パスワード %></td>
                  <td><%= f.password_field :password, autocomplete: "current-password", placeholder: "sample@example.com" %></td>
                </tr>
                <tr>
                  <td></td>
                  <td><%= f.submit "ログイン", class: "btn btn-primary my-4" %></td>
                </tr>
              </table>
              <% end %>
            </div>
          <h4 class="my-4 mr-5 pr-5"><span class="bg-light">会員登録がお済みでない方</span></h4>
        <p class="ml-5"><%= render "public/shared/links" %>から新規登録を行なってください</p>
    </div>
  </div>
</div>